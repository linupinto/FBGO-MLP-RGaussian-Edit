# A brief Over view of the programme normalization_std_cifar.py

This script normalizes the CIFAR dataset using StandardScaler, 
ensuring that the data has zero mean and unit variance for better 
performance in machine learning models.

## Key Feature
- ✅ Uses StandardScaler to normalize CIFAR data

